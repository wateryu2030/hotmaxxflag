# 好特卖报表 8946110000000000001 修复说明

## 问题

1. **cellTextJson NPE**：JimuReport 2.3.0 在处理 SQL 数据集时，部分报表会触发 `cellTextJson is null` 空指针（框架内部 bug）。
2. **API 数据集**：已改为 API 数据源，并新增 `HtmaProfitApiController` 提供 `/jmreport/api-proxy/htma/profit` 接口（支持 GET/POST）。
3. **POST 支持**：JimuReport 使用 POST 调用 API，已为接口添加 POST 支持。

## 当前状态

- API 接口正常：`curl -X POST "http://127.0.0.1:8085/jmreport/api-proxy/htma/profit?pageNo=1&pageSize=20"` 返回正确数据。
- 报表预览仍报 cellTextJson NPE，需通过设计器重新设计报表模板以规避框架 bug。

## 临时方案

在报表设计器中打开 `http://127.0.0.1:8085/jmreport/index/8946110000000000001?menuType=984272091947253760`，对报表做任意小修改（如调整列宽）后保存，可能触发框架重新生成正确的 cellTextJson 结构。

## 数据

- 数据源：`htma_dashboard.t_htma_profit`
- 接口：`/jmreport/api-proxy/htma/profit`（分页参数 pageNo, pageSize）
- 返回格式：`{"data": [...], "total": N, "count": N}`
